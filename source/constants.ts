// constants.ts

// Define the version numbers.
export const AIFORMAT_VERSION = '0.2.4'; // Incremented version number
// Define the default folders to be excluded.
export const EXCLUDED_FOLDERS = ['node_modules', '.git', 'dist', 'build', 'coverage'];
